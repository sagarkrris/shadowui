import VerticalArticle from "../../components/public/VerticalArticle"; import { getStackGuide, PUBLIC_STACK_GUIDES } from "../../lib/seoGuides.mjs"; import { getVerticalListicle, PUBLIC_VERTICAL_LISTICLES } from "../../lib/verticalContent.mjs";
export default VerticalArticle; export function getStaticPaths() { return { paths: [...PUBLIC_VERTICAL_LISTICLES.filter((x) => x.vertical === "aws"), ...PUBLIC_STACK_GUIDES.filter((x) => x.stack === "aws")].map((x) => ({ params: { slug: x.slug } })), fallback: false }; } export function getStaticProps({ params }) { return { props: { listicle: getVerticalListicle("aws", params.slug), article: getStackGuide("aws", params.slug) } }; }
