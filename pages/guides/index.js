import Head from "next/head";
import Link from "next/link";
import { PUBLIC_STACK_GUIDES } from "../../lib/seoGuides.mjs";
import { PUBLIC_VERTICAL_LISTICLES } from "../../lib/verticalContent.mjs";

export default function GuidesIndex() {
  return <><Head><title>Interview Questions by Stack | InterviewIQ</title><meta name="description" content="Structured interview preparation for Python, React, Node.js, Go, Rust, AWS, Docker, MongoDB, SQL, and Java." /><link rel="canonical" href="https://interviewiq.app/guides" /></Head><main style={{ background: "#08111f", color: "#e5edf8", minHeight: "100vh", padding: "40px 18px" }}><section style={{ margin: "0 auto", maxWidth: 1060 }}><nav style={{ color: "#8bd3ff", fontSize: 12, marginBottom: 22 }}><Link href="/">InterviewIQ</Link> / Interview guides</nav><div style={{ color: "#72d0a6", fontSize: 12, fontWeight: 900, letterSpacing: ".08em" }}>INTERVIEW PREP BY STACK</div><h1 style={{ fontSize: "clamp(30px, 6vw, 54px)", lineHeight: 1.06, margin: "10px 0" }}>Answers that are easy to study and easy to say</h1><p style={{ color: "#a9bad1", fontSize: 18, lineHeight: 1.55, maxWidth: 760 }}>Pick your stack, learn the mental model, and practice an answer with clear trade-offs.</p><h2 style={{ marginTop: 30 }}>Deep-dive guides</h2><div style={{ display: "grid", gap: 12, gridTemplateColumns: "repeat(auto-fit, minmax(280px, 1fr))" }}>{PUBLIC_STACK_GUIDES.map((guide) => <article key={guide.slug} style={{ background: "#101d30", border: "1px solid #253b57", borderRadius: 9, padding: 16 }}><div style={{ color: "#8bd3ff", fontSize: 11, fontWeight: 900, textTransform: "uppercase" }}>{guide.label}</div><h3 style={{ fontSize: 20, lineHeight: 1.2, margin: "7px 0" }}><Link href={`/guides/${guide.stack}/${guide.slug}`} style={{ color: "#f8fbff" }}>{guide.title}</Link></h3><p style={{ color: "#a9bad1", lineHeight: 1.5 }}>{guide.description}</p></article>)}</div><h2 style={{ marginTop: 30 }}>Top-question lists</h2><div style={{ display: "flex", flexWrap: "wrap", gap: 9 }}>{PUBLIC_VERTICAL_LISTICLES.map((item) => <Link key={item.slug} href={item.vertical === "java" ? `/java/listicle/${item.slug}` : `/${item.vertical}/${item.slug}`} style={{ border: "1px solid #38516e", borderRadius: 7, color: "#dbeafe", padding: "10px 12px", textDecoration: "none" }}>{item.title}</Link>)}</div></section></main></>;
}
