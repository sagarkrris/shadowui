import Link from "next/link";
import ReaderLayout from "../../components/reader/ReaderLayout";
import { READING_SERIES, readingItem } from "../../lib/readerEditorial.mjs";
import styles from "../../styles/Reader.module.css";
export default function SeriesIndex() { return <ReaderLayout title="Reading paths" description="Connected Java and backend lessons, prerequisites, and practical investigations."><h1>Build understanding in layers.</h1><p>Each path connects an explanation to a failure you can investigate. Reading times are estimates, not deadlines.</p><article className={styles.card}><h2><Link href="/build/java-dependency-injection">Build a small Java dependency-injection container</Link></h2><p>Four hands-on chapters, editable Java, downloadable tests, and a dependency-graph model. About 45 minutes. Requires Java classes, interfaces, maps, and exceptions.</p></article><article className={styles.card}><h2><Link href="/time-machine/database-decisions">Engineering Time Machine: database decisions</Link></h2><p>Three turning points. Compare the costs of changing a design with the cost of keeping it.</p></article><div className={styles.grid}>{READING_SERIES.map(series => <article className={styles.card} key={series.slug}><h2><Link href={`/series/${series.slug}`}>{series.title}</Link></h2><p>{series.description}</p><p><strong>Before you start:</strong> {series.prerequisites}</p><p>{series.items.reduce((total, href) => total + readingItem(href).minutes, 0)} minutes · {series.items.length} steps</p></article>)}</div></ReaderLayout>; }
