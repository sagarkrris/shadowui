import Link from "next/link";
import { useState } from "react";
import ReaderLayout from "../components/reader/ReaderLayout";
import { EDITORIAL_PROMISE, READER_ARTICLES, READING_SERIES } from "../lib/readerEditorial.mjs";
import { DETECTIVE_CASES } from "../lib/productionDetective.mjs";
import styles from "../styles/Reader.module.css";
export default function ReadingHome() {
  const [query, setQuery] = useState("");
  const articles = READER_ARTICLES.filter(article => `${article.title} ${article.description} ${article.category}`.toLowerCase().includes(query.toLowerCase().trim()));
  return <ReaderLayout title="Java & backend field notes" description={EDITORIAL_PROMISE}><section className={styles.hero}><p className={styles.eyebrow}>WORKING IDEAS FOR WORKING ENGINEERS</p><h1>{EDITORIAL_PROMISE}</h1><p className={styles.lead}>Read a clear explanation. Follow the evidence in a production mystery. Test the idea, then take it back to your own code.</p><div className={styles.actions}><Link className={styles.button} href="/java/hashmap-internals">Start with HashMap →</Link><Link href="/detective">Investigate a five-minute case</Link><Link href="/practice">Resume unfinished practice</Link></div><p className={styles.small}>Browse without an account. Sources, verification limits, and public corrections accompany the work.</p></section><section className={styles.card}><p className={styles.eyebrow}>ENGINEERING SYMPTOMS ATLAS</p><h2>Something is slow, stuck, or happening twice.</h2><p>Start with one of ten Java/Spring symptoms. Follow the evidence into a lesson, scenario, and practice exercise.</p><Link href="/symptoms">Search engineering symptoms →</Link></section><section aria-labelledby="article-list"><h2 id="article-list">Find your next explanation</h2><label>Search articles<input type="search" value={query} onChange={e => setQuery(e.target.value)} placeholder="Try transactions, SQL, or memory" /></label><p role="status" className={styles.small}>{articles.length} articles</p><div className={styles.grid}>{articles.map(article => <article className={styles.card} key={article.href}><p className={styles.eyebrow}>{article.category} · {article.minutes} min read</p><h3><Link href={article.href}>{article.title}</Link></h3><p>{article.description}</p></article>)}</div></section><section className={styles.card}><p className={styles.eyebrow}>BUILD A TINY SYSTEM</p><h2>Understand dependency injection by building it.</h2><p>Four Java chapters. A new requirement breaks each design, and runnable tests guide the next version.</p><Link href="/build/java-dependency-injection">Build a small Java container →</Link></section><section className={styles.card}><p className={styles.eyebrow}>ENGINEERING TIME MACHINE</p><h2>What changes when your constraints change?</h2><p>Revisit a database decision through traffic growth, regional failure, and deletion requirements.</p><Link href="/time-machine/database-decisions">Explore the database time machine →</Link></section><section><h2>Learn in a useful order</h2><div className={styles.grid}>{READING_SERIES.map(series => <article className={styles.card} key={series.slug}><h3><Link href={`/series/${series.slug}`}>{series.title}</Link></h3><p>{series.description}</p><p className={styles.small}>{series.items.length} connected articles and challenges</p></article>)}</div></section><section><h2>Take the on-call chair</h2><div className={styles.grid}>{DETECTIVE_CASES.slice(0, 3).map(incident => <article className={styles.card} key={incident.slug}><p className={styles.eyebrow}>PRODUCTION DETECTIVE · 5 MINUTES</p><h3><Link href={`/detective/${incident.slug}`}>{incident.title}</Link></h3><p>{incident.description}</p></article>)}</div></section><section className={styles.card}><h2>A small, useful digest</h2><p>One explanation, one challenge, and one practical takeaway. Read the first issue before deciding whether to subscribe.</p><Link href="/digest">Preview the digest →</Link></section></ReaderLayout>;
}
// Keep existing workspace deep links working after introducing the public homepage.
export function getServerSideProps({ query }) {
  if (query.workspace) return { redirect: { destination: `/practice?${new URLSearchParams(query).toString()}`, permanent: false } };
  return { props: {} };
}
