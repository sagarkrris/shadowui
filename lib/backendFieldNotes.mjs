export const BACKEND_FIELD_NOTES = [
  {
    id: 'request-timed-out-did-payment-happen',
    title: 'The request timed out. Did the payment happen?',
    category: 'Distributed systems', format: 'field-note', minutes: 8,
    summary: 'A timeout describes what the caller observed. Trace a lost response, preserve operation identity, and choose a safe next step.',
    updated: '2026-09-18', runtime: 'Python 3.13.5 · standard-library model',
    fixture: 'payment-timeout.py', expected: 'Lost response, same-key retry: 1 charge\nNew-key retry: 2 charges\nChanged parameters rejected; tenant scope isolated',
    scope: 'A deterministic in-memory payment-provider model, with no network or money movement. It does not test Stripe, concurrency, key expiry, or durable storage. The Java retry project separately explores scheduling.',
    overview: 'Your checkout request exceeded its deadline. That does not tell you whether the payment service rejected it, is still processing it, or accepted it before the response was lost. Treat the outcome as unknown until you have authoritative evidence. Preserve the identity of the intended operation while reconciling or retrying under the provider’s contract.',
    chapters: [
      { order: 1, title: 'Separate the operation from the response', lesson: 'Imagine a service that records a charge, then loses its connection while replying. The client sees the same timeout it would see if the request never arrived. A client deadline limits waiting; it does not, by itself, reverse an accepted effect. Record an operation ID before making the request and correlate it with provider references, status queries, or authenticated event delivery.', example: 'operation: order-104/payment-1\nclient observation: deadline exceeded\nprovider outcome: unknown until reconciled', exercise: 'List two server outcomes consistent with the same client timeout. Which observation would distinguish them?' },
      { order: 2, title: 'Keep one identity for one intended effect', lesson: 'In this fictional checkout, retries for the same payment reuse the same tenant-scoped key and unchanged parameters. A second intended payment receives a different key. The provider must own the relationship between the key and its effect; putting a key in a request header is insufficient if nothing enforces it. A local “already attempted” flag cannot atomically guard a remote charge.', example: 'attempt 1: key=order-104/payment-1, amount=2500\nattempt 2: key=order-104/payment-1, amount=2500\nchanged amount: reconcile or create a deliberately new operation', exercise: 'Identify where you would persist the operation ID and how you would prevent two workers from inventing different IDs for the same payment.' },
      { order: 3, title: 'Retry within the actual provider contract', lesson: 'For Stripe’s idempotent POST requests, repeated use of a retained key replays the saved result, including a saved 500 response. Validation failures and certain concurrent conflicts do not create that saved result; parameters are checked for consistency. Keys may be pruned after at least 24 hours. These are provider-specific semantics, not a permanent exactly-once guarantee. For uncertain outcomes, consult the provider’s recovery guidance and reconcile before treating a fresh key as harmless.', example: 'unknown outcome → inspect operation/provider reference\nretry permitted → same key + same parameters + bounded budget\nretention window exceeded → reconcile before issuing a new effect', exercise: 'Write a recovery policy for a payment that remains uncertain beyond the key-retention window. Include manual review and the user-visible status.' },
    ],
    trace: [
      ['0 ms', 'Client sends payment with a stable key.', 'One business intent gets one operation identity.'],
      ['80 ms', 'Provider records the charge and its idempotent result.', 'The effect exists before the client knows it.'],
      ['100 ms', 'Response is lost; client later reaches its deadline.', 'Unknown outcome does not mean failed effect.'],
      ['Retry', 'Same key and parameters return the earlier result in this model.', 'No second charge is created while the ledger entry remains.'],
    ],
    misconception: { question: 'The client timed out. Which conclusion is justified?', options: ['The payment definitely failed.', 'The payment outcome is unknown from that observation alone.', 'A new idempotency key makes the retry safer.'], correct: 1, feedback: ['The provider may have accepted the payment before the response was lost.', 'Correct. Reconcile the operation, then retry only within the provider’s documented semantics.', 'A fresh key can describe a new operation and create another charge.'] },
    interviewQuestions: ['A payment request timed out after the provider committed. Explain your next steps without creating a duplicate charge.', 'Which failures can an idempotency ledger prevent, and which require reconciliation?'],
    related: [{ href: '/build/retry-scheduler', title: 'Build a retry scheduler' }, { href: '/detective/the-service-that-drowned-in-retries', title: 'Investigate a retry storm' }, { href: '/symptoms/messages-processed-twice', title: 'Diagnose repeated message processing' }],
    references: [{ title: 'Stripe: idempotent requests', href: 'https://docs.stripe.com/api/idempotent_requests' }, { title: 'Stripe: low-level error handling', href: 'https://docs.stripe.com/error-low-level' }],
  },
  {
    id: 'connection-pool-full-is-database-slow',
    title: 'Your connection pool is full. Is the database actually slow?',
    category: 'Java performance', format: 'field-note', minutes: 8,
    summary: 'Separate time waiting to acquire a connection from time executing SQL, then distinguish long ownership, leaks, locks, and excess demand.',
    updated: '2026-09-18', runtime: 'Python 3.13.5 · standard-library model',
    fixture: 'pool-ownership.py', expected: 'SQL time: 5 ticks; connection hold: 100 ticks\nThird caller waits: 100 ticks\nShorter ownership waits: 5 ticks\nNever-returned resources exhaust acquisition',
    scope: 'A deterministic two-resource timeline using virtual ticks, not JDBC or a HikariCP runtime. It isolates ownership duration; it does not estimate real throughput or a safe production pool size.',
    overview: 'A full pool tells you that the application has no free connection at that moment. It does not identify why. Instrument acquisition wait, connection hold duration, SQL execution, and database waits separately. The bottleneck may be a query, a lock, application work performed while holding a connection, an unreturned lease, or an arrival rate the available capacity cannot serve.',
    chapters: [
      { order: 1, title: 'Measure three different intervals', lesson: 'Start the acquisition timer immediately before requesting a connection and stop it when the request succeeds or fails. Measure ownership from acquisition to return. Measure individual SQL calls inside that ownership interval. In a fictional trace, a query takes five milliseconds while the application holds its connection through a slow remote call. Calling that entire interval “database latency” would send the investigation in the wrong direction.', example: 'acquire wait: 800 ms\nconnection held: 1200 ms\nSQL execution: 5 ms\nremote call while holding connection: 1100 ms', exercise: 'Draw the intervals from your own trace. Identify whether your query timer includes pool acquisition and whether time spent waiting on database locks is visible.' },
      { order: 2, title: 'Use evidence to separate causes', lesson: 'Compare competing explanations. Slow SQL is supported by long statement spans and an execution plan or database wait event explaining the delay. Long application ownership is supported by short statements inside much longer lease spans. A leak remains plausible when checkout records have no matching return after the request ends. Excess demand is plausible when leases return normally but acquisition queues grow during bursts. These can coexist; low CPU alone rules out none of them.', example: 'long query + lock wait → inspect blocker and transaction age\nshort query + long lease → inspect work inside ownership boundary\ncompleted request + unreturned lease → inspect all exit paths\nnormal lease times + burst queue → examine admission and capacity', exercise: 'For each hypothesis, name one observation that would weaken it. Do not use a full-pool metric as evidence for every cause.' },
      { order: 3, title: 'Repair ownership before guessing capacity', lesson: 'HikariCP’s connectionTimeout limits waiting for a connection from the pool; it is not a SQL execution deadline. Its leak detector reports a possible leak after a configured checkout duration, so a legitimate long operation can also trigger it. Once you identify the cause, shorten unnecessary ownership, close resources on every path, address the query or lock, or bound incoming work. Evaluate any pool-size change under representative database load; allowing more concurrent SQL may move or worsen the bottleneck.', example: 'acquire → minimal database work → commit/rollback → return\nthen perform independent remote work, if business semantics permit', exercise: 'Propose one measured experiment. State the invariant that prevents moving a remote call outside a transaction blindly, and the latency and database-load metrics you will compare.' },
    ],
    trace: [
      ['0', 'Two callers acquire the two available resources.', 'The pool is at capacity.'],
      ['5', 'Their SQL finishes, but ownership continues.', 'Fast statements do not imply free connections.'],
      ['0–100', 'A third caller waits while the first two hold their resources.', 'The queue persists long after the first two statements finish in this model.'],
      ['100', 'One resource returns; the queued caller can acquire it.', 'The waiting time depended on return time, not SQL duration alone.'],
    ],
    misconception: { question: 'A leak-detection warning is present. What should you conclude?', options: ['The connection is certainly never returned.', 'The SQL execution deadline expired.', 'A connection has been held long enough to warrant investigating its ownership.'], correct: 2, feedback: ['A long but eventually returned lease can also trigger the warning.', 'Pool acquisition, checkout duration, and SQL execution are different intervals.', 'Correct. Correlate the warning with checkout/return records and the request lifecycle.'] },
    interviewQuestions: ['CPU is low, the pool is full, and requests are slow. What do you measure first, and how do you distinguish the causes?', 'When could increasing maximumPoolSize make service latency worse?'],
    related: [{ href: '/build/connection-pool', title: 'Build a connection pool model' }, { href: '/symptoms/low-cpu-slow-requests', title: 'Investigate low CPU and slow requests' }, { href: '/learn/spring-transactions', title: 'Trace Spring transaction boundaries' }],
    references: [{ title: 'HikariCP: acquisition timeout, pool sizing and leak detection', href: 'https://github.com/brettwooldridge/HikariCP#configuration-knobs-baby' }, { title: 'PostgreSQL 18: monitoring database activity', href: 'https://www.postgresql.org/docs/18/monitoring-stats.html' }],
  },
  {
    id: 'database-committed-what-survives-crash',
    title: 'The database said “committed.” What survives a crash?',
    category: 'Database reliability', format: 'field-note', minutes: 9,
    summary: 'Distinguish visibility, local log durability, and remote recovery before turning a commit acknowledgement into a promise.',
    updated: '2026-09-18', runtime: 'Python 3.13.5 · standard-library model',
    fixture: 'commit-durability.py', expected: 'Before local flush: acknowledged write lost in this model\nAfter local flush: write recovered\nRemote lag: failover loses the local-only write\nAfter remote flush: write recovered on the standby',
    scope: 'A virtual log with explicitly separate buffered, locally durable and remotely durable state. A simulated crash discards buffers. No real disk, PostgreSQL instance, filesystem flush, or hardware failure is tested.',
    overview: 'A commit acknowledgement has meaning under a configured durability contract. Ask which failure you need to survive: a client disconnect, database process crash, loss of the primary machine, or loss of a region. Then identify where the log is durably stored and what the server waited for before acknowledging. Visibility on a live primary, local persistence, and persistence on a failover target are distinct observations.',
    chapters: [
      { order: 1, title: 'Identify the acknowledgement boundary', lesson: 'Write-ahead logging lets a database reconstruct changes from durable log records even when modified data pages have not all reached storage. The crucial question is what crossed a durable boundary before acknowledgement. For PostgreSQL 18, synchronous_commit=off can acknowledge before local WAL is flushed; a crash may lose recent acknowledged transactions without making this mode equivalent to disabling fsync. Keep the durability setting and the failure model in the incident record.', example: 'change prepared → WAL buffered → WAL flushed → acknowledgement\nasync acknowledgement may occur before the flush boundary', exercise: 'Write down the effective session-level commit setting and storage assumptions for a service. Which observed response demonstrates persistence, and which merely demonstrates visibility?' },
      { order: 2, title: 'Separate local recovery from failover', lesson: 'A locally durable commit does not by itself prove that another machine has received the corresponding log. PostgreSQL streaming replication is asynchronous by default. With a configured synchronous standby, the selected synchronous_commit mode determines whether acknowledgement waits for remote write, remote flush, or replay. remote_write does not promise survival of a standby operating-system crash; remote_apply additionally waits for replay. Name both the mode and standby configuration instead of saying “replication is enabled.”', example: 'primary durable position: 42\nstandby durable position: 41\nprimary lost → standby cannot recover entry 42 from its own log', exercise: 'Would your proposed failover target contain every acknowledged order? Identify the evidence needed and how loss of the synchronous standby changes availability.' },
      { order: 3, title: 'Test the promise you actually need', lesson: 'For a fictional order service, write an acceptance statement such as “After losing the primary machine, the designated failover target contains every acknowledged order.” Build a failure-injection test around acknowledged operation IDs, the intended failover procedure, and reads on the recovered target. Also test client disconnects around commit: a transaction may commit even when the caller never receives the acknowledgement. Durable storage and retry safety solve different parts of that incident.', example: 'record acknowledged IDs → inject the chosen failure\nrecover the intended target → compare IDs and invariants\ninspect ambiguous client operations separately', exercise: 'Contrast process termination with machine loss. Explain why a successful process-restart test cannot establish protection against loss of the only durable disk.' },
    ],
    trace: [
      ['A', 'An assignment is appended to a volatile log buffer.', 'Visibility in this model is not yet durability.'],
      ['B', 'The local log is flushed; the primary can recover it.', 'Assumes the local durable storage survives the chosen failure.'],
      ['C', 'The standby has not yet flushed this record.', 'Failing over now can lose the locally durable write.'],
      ['D', 'The designated standby flushes the record.', 'That target can recover it in this simplified model.'],
    ],
    misconception: { question: 'A write is locally durable and asynchronous replication is enabled. Is it guaranteed on the standby after primary loss?', options: ['Yes, because replication is enabled.', 'No; the standby may not have received and durably stored that write.', 'Yes, if the original client used a longer timeout.'], correct: 1, feedback: ['Replication configuration alone does not establish the standby’s durable position.', 'Correct. Verify the acknowledgement contract and the target’s log state.', 'Client waiting time cannot substitute for a replication durability guarantee.'] },
    interviewQuestions: ['What does a commit acknowledgement guarantee under your database configuration, and which failure would invalidate your assumption?', 'Why are durable commits and safe retries separate concerns?'],
    related: [{ href: '/build/write-ahead-log', title: 'Build a write-ahead log model' }, { href: '/time-machine/database-decisions', title: 'Revisit a database decision after regional failure' }, { href: '/tech-blogs/request-timed-out-did-payment-happen', title: 'Reconcile a timed-out operation' }],
    references: [{ title: 'PostgreSQL 18: WAL introduction', href: 'https://www.postgresql.org/docs/18/wal-intro.html' }, { title: 'PostgreSQL 18: asynchronous commit', href: 'https://www.postgresql.org/docs/18/wal-async-commit.html' }, { title: 'PostgreSQL 18: synchronous_commit modes', href: 'https://www.postgresql.org/docs/18/runtime-config-wal.html#GUC-SYNCHRONOUS-COMMIT' }, { title: 'PostgreSQL 18: synchronous replication', href: 'https://www.postgresql.org/docs/18/warm-standby.html#SYNCHRONOUS-REPLICATION' }],
  },
];
