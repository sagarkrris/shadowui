// Original teaching cases: illustrative constraints, not employer interview reports.
export const ADVANCED_READINGS = [
  { title: 'Anthropic · Demystifying evals for AI agents', url: 'https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents', note: 'Background reading on evaluation design; numerical exercises here are original teaching examples.' },
  { title: 'Google Cloud · Optimizing RAG retrieval', url: 'https://cloud.google.com/blog/products/ai-machine-learning/optimizing-rag-retrieval', note: 'Background reading on retrieval measurement; workshop solutions depend on the stated constraints.' },
];

export const ADVANCED_WORKSHOPS = [
  {
    id: 'advanced-retrieval', title: 'Production RAG: ranking, freshness, and access', level: 'Intermediate → Senior', prerequisite: 'Core modules 2–4; sets, sorting, and basic search metrics.',
    objective: 'Design and evaluate retrieval that finds the right evidence without exposing unauthorized or deleted documents.',
    concepts: [
      { title: 'Chunking is an information-design decision', text: 'Preserve headings, table context, and source offsets so a chunk is independently interpretable. Small chunks can lose qualifications; large chunks can dilute relevance and consume context. Overlap reduces boundary loss but duplicates evidence. Label representative queries before comparing configurations; keep the generation model fixed while changing retrieval.' },
      { title: 'Hybrid retrieval and reranking', text: 'Lexical search is useful for exact error codes and identifiers; dense retrieval can recover paraphrases. Retrieve authorized candidates from both, deduplicate by stable chunk ID, and fuse ranks before reranking a bounded candidate set. Reciprocal rank fusion sums 1/(k + rank) across lists; absent documents contribute zero. Raw cosine and lexical scores are generally not directly comparable.' },
      { title: 'Freshness is part of correctness', text: 'Track document version, deletion state, tenant, and access metadata. A stale vector index or answer cache can outlive permission changes. Recheck candidate authorization against authoritative state before context construction, invalidate caches on changes, and define a maximum tolerated indexing delay. Avoid returning stale sensitive content merely to preserve availability.' },
    ],
    workedExample: 'Lexical ranking is [A,B,C] and dense ranking is [B,D,A]. With k=60, A scores 1/61 + 1/63 ≈ 0.032266, B scores 1/62 + 1/61 ≈ 0.032522, C scores 1/63 ≈ 0.015873, and D scores 1/62 ≈ 0.016129. The fused order is B,A,D,C. If only A and D are relevant, recall@2 is 1/2 and precision@2 is 1/2. Ranking B first does not make it correct: the relevance labels still decide quality.',
    dialogue: [['Student', 'Can reranking fix a document missing from both candidate lists?'], ['Teacher', 'No. Candidate recall is an upper bound on what a reranker can recover. Diagnose retrieval misses before tuning the final prompt.']],
    exercise: 'Extend the core lab on paper with two ranked ID lists, relevance labels, a revoked document, and a deleted version. Implement rank fusion, then design an authoritative filtering step. Compare top-2 results before and after revocation and explain how the answer cache is invalidated.',
    starter: 'const lexical = ["A", "B", "C"];\nconst dense = ["B", "D", "A"];\nconst relevant = new Set(["A", "D"]);\n// Sum reciprocal ranks, sort descending, then evaluate against labels.',
    solution: 'Accumulate rank contributions in a Map keyed by stable ID and use a deterministic tie-break. Given both lists, select B,A. If B is revoked, remove it before context assembly and select A,D from the remaining fused candidates: precision@2 and recall@2 both become 1 for this fixture. In production, filter unauthorized candidates as early as possible and recheck before use; a toy post-fusion filter alone is not a complete authorization design.',
    acceptance: ['Computes B,A,D,C with a deterministic tie-break.', 'Explains why reranking cannot repair missing candidates.', 'Tests revocation, deletion, and cache invalidation without exposing revoked text.'],
    homework: 'Label 20 queries across exact identifiers, paraphrases, missing evidence, and stale documents. Submit chunking comparisons, retrieval metrics, and one failure that generation cannot repair.',
    misconception: 'A higher similarity score is a calibrated probability of correctness. It is a ranking signal and must be evaluated against task-specific relevance.',
  },
  {
    id: 'advanced-evaluation', title: 'Evaluation engineering and release decisions', level: 'Senior', prerequisite: 'Core module 4; proportions, test fixtures, and experiment controls.',
    objective: 'Separate improvements from noise and make a defensible release decision when aggregate and safety metrics disagree.',
    concepts: [
      { title: 'Evaluate behavior and outcomes', text: 'A tool transcript can claim success without the intended database state existing. Use deterministic checks for concrete state transitions, task rubrics for open-ended answers, and human review for ambiguous judgments. Calibrate any model grader against independently labeled examples and inspect disagreements instead of treating its score as ground truth.' },
      { title: 'Control the comparison', text: 'Run baseline and candidate on the same cases with versioned prompts, corpus snapshots, model settings, and tools. Keep development and held-out cases separate. Repeated trials expose run-to-run instability; report denominators and segment results. Pair cases when comparing versions so unrelated case difficulty does not dominate the comparison.' },
      { title: 'Release gates and statistical limits', text: 'Predefine which errors are unacceptable and which metrics are trade-offs. A small sample cannot establish safety for rare events. Under independent, representative Bernoulli trials with zero failures, the rough rule of three gives an approximate 95% upper failure-rate bound of 3/n. This approximation does not compensate for biased fixtures or correlated trials.' },
    ],
    workedExample: 'Across 100 paired cases, baseline passes 84 and candidate passes 90. Eight cases improve but two regress, including one cross-tenant disclosure. The aggregate gain is six percentage points, but the candidate fails a zero-disclosure release gate. Even 0 failures in 100 independent trials would give only an approximate upper bound of 3%, not evidence of zero production risk.',
    dialogue: [['Student', 'Can a 90% score justify shipping despite one privacy failure?'], ['Teacher', 'Not under the stated hard privacy gate. Diagnose and repair that failure, then rerun the affected adversarial cases and the broader comparison.']],
    exercise: 'Construct a paired result table with eight improvements and two regressions. Separate schema checks, claim support, privacy, and latency. Write the release decision and design a follow-up experiment that addresses the disclosure instead of hiding it in an average.',
    starter: 'const paired = { improved: 8, regressed: 2, unchangedPass: 82, unchangedFail: 8 };\n// Baseline passes: regressed + unchangedPass.\n// Candidate passes: improved + unchangedPass.',
    solution: 'Baseline is 2+82=84; candidate is 8+82=90; n=100. Block the release because the privacy gate fails. Preserve the failing case as a regression fixture, independently check the authorization fix, then repeat the paired evaluation. Use additional representative cases to investigate quality changes; do not claim that this small dataset establishes production safety.',
    acceptance: ['Reports paired denominators and six-point net gain.', 'Blocks release on the hard privacy gate.', 'Distinguishes deterministic checks, human calibration, and sampling uncertainty.'],
    homework: 'Create a versioned evaluation manifest and rubric. Include a grading disagreement, multiple trials of a nondeterministic case, and a rollback criterion that is independent of the average score.',
    misconception: 'An LLM judge removes the need for human labels. A grader can be biased or reward fluent but unsupported answers.',
  },
  {
    id: 'advanced-serving', title: 'Inference, streaming, capacity, and cost', level: 'Intermediate → Senior', prerequisite: 'Core modules 1 and 4; HTTP, latency, and basic concurrency.',
    objective: 'Reason about serving bottlenecks before choosing caching, batching, smaller models, or shorter outputs.',
    concepts: [
      { title: 'Prefill and decode', text: 'Prefill processes the prompt; decode generates successive tokens. KV caching reuses attention state within generation, but consumes memory that grows with active sequences and context length. Ordinary dense attention has quadratic pairwise work in sequence length; practical kernels and model architectures change resource behavior. Measure your serving stack instead of treating a complexity formula as a latency prediction.' },
      { title: 'Streaming changes perceived latency', text: 'Measure time to first token separately from time to complete and time to a validated result. Streaming lets users see progress, but a partial JSON object is not a validated action. Handle disconnects, cancellation, backpressure, and timeouts. Cancelling the browser view does not necessarily cancel provider work or billing; propagate cancellation where supported.' },
      { title: 'Capacity and cache boundaries', text: 'For a stable system, average concurrency is approximately arrival rate multiplied by average time in the system. Include queue time and retries when choosing capacity. Caching can reduce work, but keys must include relevant tenant, policy, corpus, prompt, and model versions; invalidate sensitive results on access changes. Quantization and batching require quality and latency measurement, not assumptions.' },
    ],
    workedExample: 'At 20 requests/second and mean end-to-end time of 3 seconds, average in-flight requests are about 60. This is not enough information to predict p95 latency or peak capacity. A 1,000-token prompt and 250-token response cost $0.002 at illustrative $1/M input and $4/M output rates. If 10% of requests retry exactly once with identical sizes, expected cost is $0.0022/request, excluding retrieval and infrastructure.',
    dialogue: [['Student', 'If we stream immediately, is the operation fast enough?'], ['Teacher', 'Only if your user requirement concerns visible progress. A validated plan can still arrive too late; measure both first-token and completed-result latency.']],
    exercise: 'Sketch a request timeline with queue, retrieval, prefill, decode, and validation. Calculate concurrency and expected retry cost, then propose one change for long prompts and another for excessive output. Define measurements that would disprove your choices.',
    starter: 'const requestsPerSecond = 20;\nconst meanSeconds = 3;\nconst retryFraction = 0.10;\n// Compute average in-flight requests and expected attempts.',
    solution: 'Average in-flight work is 60 and expected attempts are 1.1. At 10,000 requests the illustrative model cost is $22. Reduce irrelevant input or use measured prefix caching for prompt-heavy work; cap unnecessary output for decode-heavy work. Re-evaluate quality after either change. Bound queues and reject overload rather than retrying indefinitely.',
    acceptance: ['Computes concurrency 60 and expected cost $22/10,000 requests.', 'Separates first-token from validated-result latency.', 'Explains authorization-aware caching, cancellation limits, and bounded overload behavior.'],
    homework: 'Write a load-test plan with burst traffic, long contexts, slow clients, and provider throttling. Report hypothetical SLOs clearly as targets; do not invent benchmark measurements.',
    misconception: 'Streaming reduces total model work. It changes delivery timing; the generated token count and provider behavior determine work and cost.',
  },
  {
    id: 'advanced-tools', title: 'Durable agents and reliable tool execution', level: 'Senior', prerequisite: 'Core module 5; transactions, unique constraints, and state machines.',
    objective: 'Design recovery for a tool call whose outcome is unknown without duplicating a side effect or reusing stale approval.',
    concepts: [
      { title: 'Proposals and authorization are different', text: 'Treat a tool request as an untrusted proposal. Validate schema and authorization independently, bind approval to actor and canonical payload version, and reject stale approval. A tool protocol or well-formed schema describes invocation; it does not grant authority to perform the action.' },
      { title: 'Durability and deduplication', text: 'For a side effect in one database, record a scoped operation key and payload hash atomically with the write. A unique constraint prevents concurrent duplicates. A retry with the same payload returns the recorded result; a changed payload conflicts. Expiry and retention must cover the expected retry window.' },
      { title: 'External side effects', text: 'A database transaction cannot atomically commit an arbitrary external provider call. Use a durable intent/outbox, provider idempotency where available, and reconciliation of unknown outcomes. If the provider offers neither lookup nor deduplication, a timeout can require manual recovery rather than a blind retry. Exactly-once delivery is not a generic HTTP property.' },
    ],
    workedExample: 'Two workers receive operation K for the same approved study-plan save. A unique (tenant, operationKey) constraint allows only one to commit the local write. The other reads the recorded result. If K is replayed with a different duration, reject it. For an external calendar creation, a timeout after provider acceptance leaves the outcome unknown; reconcile the provider operation before allowing a replacement event.',
    dialogue: [['Student', 'Can an in-memory lock solve duplicates?'], ['Teacher', 'It cannot coordinate replicas or survive a crash. Put the invariant in durable shared state and define what happens across the external boundary.']],
    exercise: 'Draw states proposed, approved, executing, succeeded, failed, and unknown. Design a table with a unique operation key, actor, payload hash, status, and result. Simulate two concurrent requests, a crash after commit, and an external timeout.',
    starter: '-- Design sketch, not a complete migration:\n-- UNIQUE (tenant_id, operation_key)\n-- actor_id, payload_hash, status, result, expires_at\n-- Commit local side effect and operation result in one transaction.',
    solution: 'Use durable uniqueness plus a transaction for the local write. Reject conflicting hashes and stale or revoked authorization before execution. A crashed worker can be retried by reading committed state. For external writes, persist intent and reconcile via a provider key; do not equate a missing HTTP response with failure. Surface an unknown state to the user when reconciliation cannot establish the outcome.',
    acceptance: ['Covers concurrent duplicate requests and changed payloads.', 'Separates local atomicity from external-provider uncertainty.', 'Defines safe recovery and user-visible unknown outcomes.'],
    homework: 'Write a fault-injection matrix for crashes before intent, after intent, after provider acceptance, and before acknowledgment. Explain the recovery owner and user message for each point.',
    misconception: 'Adding a UUID guarantees exactly-once execution. The receiving service must enforce deduplication durably and within a defined scope.',
  },
  {
    id: 'advanced-adaptation', title: 'Prompting, RAG, fine-tuning, and model routing', level: 'Intermediate → Senior', prerequisite: 'Core modules 1–4; train/test separation and labeled examples.',
    objective: 'Choose an adaptation strategy based on the source of errors and prove that it improves the intended task.',
    concepts: [
      { title: 'Knowledge versus behavior', text: 'Retrieval supplies changing external evidence without retraining the model. Fine-tuning changes learned behavior and can improve task format or style when representative examples exist. It does not automatically keep facts current, enforce access control, or eliminate hallucination. Diagnose whether failure comes from missing evidence, poor instructions, or behavior before selecting the intervention.' },
      { title: 'Training data and leakage', text: 'Supervised fine-tuning learns from input–target examples; parameter-efficient approaches such as LoRA learn smaller low-rank updates rather than changing all weights. Smaller trainable parameter counts do not remove the need for privacy review, representative data, and evaluation. Split by source or time when near-duplicates would otherwise leak between training and test sets.' },
      { title: 'Routing is another decision to evaluate', text: 'A smaller model can serve easy cases and escalate difficult ones, but a router can misclassify difficulty. Model self-reported confidence is not automatically calibrated. Evaluate the complete routed system including escalations, cost, and latency; do not average standalone model scores and assume they describe routing performance.' },
    ],
    workedExample: 'Of 100 failures, 60 lack current policy evidence, 25 violate a response format, and 15 require reasoning over supplied evidence. Start with retrieval and freshness for the 60, a clearer schema and validation for the 25, and controlled model/prompt comparisons for the 15. Fine-tuning becomes a candidate only after the simpler changes leave a stable behavior gap with sufficient reviewed training examples.',
    dialogue: [['Student', 'Can fine-tuning memorize every policy and replace retrieval?'], ['Teacher', 'That makes frequent changes and provenance harder. Use fresh evidence for changing facts, then evaluate any tuning separately for the behavior you want to improve.']],
    exercise: 'Create an error taxonomy and an adaptation decision record. Propose a train/dev/test split for documents with repeated templates. Design a router evaluation with easy, hard, and falsely confident cases.',
    starter: 'const failures = { missingEvidence: 60, invalidFormat: 25, reasoning: 15 };\n// Choose an intervention per cause and one disconfirming evaluation per intervention.',
    solution: 'Do not train on the held-out policies used for final measurement. Group near-duplicates together before splitting, version data and model artifacts, and compare against the original baseline. Evaluate current-policy citation support for RAG, strict validity for formatting, and difficult-case accuracy for model changes. Require the routed system itself to satisfy the release gates.',
    acceptance: ['Matches each error category to a justified intervention.', 'Prevents near-duplicate leakage and protects private training data.', 'Evaluates router mistakes and escalations as part of the full system.'],
    homework: 'Submit a one-page decision record choosing among prompting, RAG, tuning, or routing for a concrete product. Include cost assumptions, data prerequisites, rollback, and evidence that would change your choice.',
    misconception: 'Fine-tuning is always the next step after a prototype. It is justified by a measured, repeatable behavior gap and suitable data.',
  },
  {
    id: 'advanced-incidents', title: 'Production incidents and AI code-review interviews', level: 'Senior', prerequisite: 'Core modules 5–6; logs, deployments, code review, and incident response.',
    objective: 'Contain a model-related regression, reconstruct its cause, and review the smallest safe fix with independent tests.',
    concepts: [
      { title: 'Observability without unnecessary payload retention', text: 'Record run IDs, version identifiers, timings, token usage, policy decisions, and safe error categories. Full prompts can contain private profiles or credentials. Define redaction, access, retention, and incident-specific capture explicitly. A useful trace explains system actions without requiring exposure of private internal reasoning.' },
      { title: 'Contain, reproduce, repair', text: 'Start with user impact and active data boundaries. Disable a risky tool, roll back a model/prompt/corpus bundle, or use a safe fallback when justified. Preserve minimal evidence, compare affected versions, and add the smallest reproducible failure fixture. A model upgrade, corpus change, or cache-key change can each cause the same visible symptom.' },
      { title: 'AI-assisted review needs independent expectations', text: 'Generated code and generated tests may share a mistaken assumption. Derive acceptance cases from the contract before reading the proposed tests. Check null and empty inputs, access changes, mutable shared state, timeout recovery, and old persisted values. Prefer a focused correction and a failing-before/passing-after test over a broad rewrite during an incident.' },
    ],
    workedExample: 'A cache key uses only question text. Tenant A asks “What is my study plan?” and populates the cache; tenant B receives A’s plan. Contain by disabling that shared answer cache. Use trusted tenant/user scope plus relevant corpus, policy, prompt, and model versions in the key, and invalidate on revocation. A test with two tenants and the identical question should fail before the fix and return isolated results afterward.',
    dialogue: [['Student', 'Can we patch only the cache key and close the incident?'], ['Teacher', 'Also invalidate existing unsafe entries, investigate affected requests under the incident process, and verify uncached retrieval authorization. The immediate bug may not be the only data boundary.']],
    exercise: 'Review the cache sketch below. Write a minimal failing test, a containment step, a corrected key policy, and a rollback plan. Explain how to gather evidence without exporting private prompts.',
    starter: 'const key = request.question;\nconst answer = cache.get(key);\nif (answer) return answer;\n// Bug: authorization scope and version identity are missing.',
    solution: 'Do not trust a tenant ID supplied in the body; derive scope from authenticated state. Cache only under a scope that preserves the effective permissions, include relevant versions, and invalidate unsafe old entries. Test identical questions across tenants, permission revocation, changed corpus versions, and cache misses. Keep safe request/version metadata for debugging rather than broad prompt dumps.',
    acceptance: ['Contains leakage before optimizing performance.', 'Tests trusted scope, revocation, and version changes.', 'Explains invalidation, independent test expectations, and rollback.'],
    homework: 'Write a five-minute incident presentation: impact, timeline, root cause, containment, fix evidence, and prevention. State which observations are known and which are hypotheses.',
    misconception: 'A passing generated test proves isolation. The test must actually include multiple identities, shared question text, and changing permissions.',
  },
];

export const INTERVIEW_SCENARIOS = [
  {
    id: 'rag-launch', workshopId: 'advanced-retrieval', title: 'RAG design review under changing permissions', level: 'Senior · system design', minutes: 10,
    prompt: 'Design a study assistant for 50 tenants and 100,000 documents. It must cite current evidence, isolate tenants, and meet a 2-second p95 response target. What do you clarify, build first, and measure?',
    clarifications: ['Does 2 seconds mean first token or completed validated answer?', 'How quickly must revocations and document edits take effect?', 'What queries and labeled relevance data represent real usage?'],
    conciseAnswer: 'I would clarify freshness, permission, and latency semantics, then start with authorized retrieval plus a bounded grounded-answer workflow. I would measure candidate recall separately from answer support, keep source/version provenance, and use a safe abstention path. I would only add hybrid search or reranking after measuring misses against a labeled baseline.',
    reasoning: ['Ingest with stable document/chunk IDs, source offsets, versions, and trusted access metadata.', 'Filter candidates by caller scope and recheck revocation before context construction; use bounded retrieval and generation budgets.', 'Evaluate retrieval precision/recall, citation support, abstention, and end-to-end latency by tenant/query segment.', 'Define cache invalidation, rollout gates, and a fallback for unavailable or insufficient evidence.'],
    followUp: 'A user loses access immediately after a document enters the vector index. The index updates every 10 minutes. Can the answer cache still serve it?',
    followUpAnswer: 'No, if revocation must take effect immediately. Revalidate effective access against authoritative state before using cached results or retrieved text; invalidate affected entries and exclude revoked sources. During uncertainty, fail closed for that content. Explain the availability trade-off and measure the revocation path separately from ordinary indexing freshness.',
    pitfalls: ['Treating vector metadata as permanently authoritative.', 'Claiming citations prove claim support.', 'Promising p95 without defining or measuring the complete request path.'],
    rubric: ['Clarifies completion versus first-token latency.', 'Enforces tenant access and immediate revocation.', 'Separates retrieval and answer evaluation.', 'Defines abstention, cache invalidation, and rollout evidence.'],
  },
  {
    id: 'eval-gate', workshopId: 'advanced-evaluation', title: 'A better average hides a privacy regression', level: 'Senior · evaluation', minutes: 5,
    prompt: 'A candidate model passes 90/100 paired cases versus 84/100 for the baseline. Eight improve and two regress; one regression reveals another tenant’s note. The release gate forbids cross-tenant disclosure. Do you release? Explain the evidence and next experiment.',
    clarifications: ['Were prompts, tools, corpus, and cases held constant?', 'Is the privacy gate a hard requirement?', 'Were the cases held out and independently labeled?'],
    conciseAnswer: 'I would block the release: the six-point aggregate gain does not override the hard privacy gate. I would preserve the disclosure case, trace the data boundary, repair it, and repeat both the adversarial checks and the paired evaluation. I would report denominators and uncertainty rather than describe 100 cases as proof of safety.',
    reasoning: ['Reconcile totals: 82 unchanged passes plus 2 regressions gives 84; 82 plus 8 improvements gives 90.', 'Inspect authorization, retrieval, cache, and tool boundaries before blaming model behavior alone.', 'Create a regression fixture from the disclosure and validate the repair independently.', 'Use representative segments and repeated trials to check quality stability before a staged rollout.'],
    followUp: 'After the fix, there are zero disclosures in 100 independent representative trials. Does that prove the production rate is below 0.1%?',
    followUpAnswer: 'No. The rough rule-of-three upper 95% bound is 3/100 = 3%, far above 0.1%. Approximately 3,000 zero-failure independent representative trials would be needed for a rough 0.1% bound, and those assumptions may not hold. Statistical sampling also does not replace enforced authorization boundaries.',
    pitfalls: ['Shipping because the average score improved.', 'Calling a model grader objective truth.', 'Interpreting zero observed failures as zero risk.'],
    rubric: ['Computes and explains the paired comparison.', 'Blocks on the privacy gate.', 'States sample-size and independence limits.', 'Proposes targeted repair plus broader regression evaluation.'],
  },
  {
    id: 'latency-spike', workshopId: 'advanced-serving', title: 'Streaming looks fast but completed answers are slow', level: 'Intermediate · performance', minutes: 5,
    prompt: 'The assistant handles 20 requests/second with mean completion time 3 seconds. Users see the first token quickly but complain about slow finished plans. Ten percent of requests retry once. How do you diagnose capacity, latency, and cost?',
    clarifications: ['What are first-token, complete-answer, and validated-result p95 values?', 'What time is spent in queue, retrieval, model processing, and validation?', 'Are retries duplicates, throttling, or recoverable format repairs?'],
    conciseAnswer: 'Average concurrency is about 60 in a stable system, but that is not a p95 or peak-capacity estimate. I would instrument the full timeline and distinguish long-input from long-output work, then bound queues and retries. Streaming improves perceived progress; it does not remove decode time or validation. I would compare any optimization against quality and actual usage.',
    reasoning: ['Use arrival rate × mean duration for an average in-flight estimate, then inspect burst and tail behavior.', 'Measure input/output token distributions and retry causes by version and query type.', 'Reduce irrelevant context for prompt-heavy requests or unnecessary output for decode-heavy requests; measure the effect.', 'Use cancellation, backpressure, and bounded overload responses; evaluate cache isolation before reuse.'],
    followUp: 'At illustrative $1/M input and $4/M output, each request uses 1,000 input and 250 output tokens. What does 10,000 requests cost with the stated retries?',
    followUpAnswer: 'One attempt costs $0.002. Ten percent retrying exactly once gives 1.1 expected attempts, so 10,000 requests cost $22 in model usage under equal-size attempts. Retrieval, infrastructure, cache billing differences, and differently sized retries are excluded; real usage should reconcile the estimate.',
    pitfalls: ['Confusing fast first token with fast completion.', 'Using mean concurrency as a p95 prediction.', 'Ignoring retries or assuming cancellation eliminates billing.'],
    rubric: ['Computes average concurrency of 60.', 'Separates first-token and completion timing.', 'Computes $22 with assumptions.', 'Proposes measured optimization and bounded retries.'],
  },
  {
    id: 'duplicate-calendar', workshopId: 'advanced-tools', title: 'Calendar tool times out after approval', level: 'Senior · reliability', minutes: 10,
    prompt: 'An approved calendar-creation tool times out. Two workers retry the same request, and the user edits the meeting time before the retry completes. Design the recovery and approval behavior.',
    clarifications: ['Does the provider support idempotency keys and lookup by operation ID?', 'Which payload version did the user approve?', 'Where is operation state stored, and can multiple workers execute it?'],
    conciseAnswer: 'The timeout leaves the outcome unknown. I would persist an operation scoped to actor and tenant, bind approval to the exact payload version, and deduplicate retries durably. An edit requires a new authorization decision and cannot reuse the old key with a changed payload. For the external calendar, reconcile via provider idempotency or lookup before retrying.',
    reasoning: ['Validate a typed proposal and authenticated approval independently of model output.', 'Use a unique scoped operation key and canonical payload hash; identical retries read the recorded result.', 'Persist intent and reconcile external-provider state because a local transaction cannot cover the remote side effect.', 'Expose pending or unknown status and bound automated recovery instead of silently creating another event.'],
    followUp: 'The provider has neither idempotency nor lookup. The first request may have succeeded. Is blind retry acceptable?',
    followUpAnswer: 'Not if duplicate creation is unacceptable. Stop automatic retries, retain the unknown state, and use an operator or user reconciliation flow with a clear duplicate-risk explanation. Document the provider limitation and consider a different integration contract. A UUID in our request cannot make a provider deduplicate when it does not implement that behavior.',
    pitfalls: ['Treating timeout as proof of failure.', 'Relying on an in-memory lock across replicas.', 'Reusing approval after payload edits or promising generic exactly-once HTTP execution.'],
    rubric: ['Recognizes uncertain outcomes.', 'Binds approval to actor and payload version.', 'Handles concurrency and changed-key payload conflicts.', 'Provides safe recovery without provider deduplication.'],
  },
  {
    id: 'tuning-decision', workshopId: 'advanced-adaptation', title: 'Fine-tune or repair the evidence pipeline?', level: 'Intermediate · model strategy', minutes: 5,
    prompt: 'A team wants to fine-tune after 100 failures: 60 lack current policy evidence, 25 use the wrong JSON format, and 15 reason incorrectly over supplied facts. Policies change weekly. What do you recommend?',
    clarifications: ['Was the required evidence available in the input for each failure?', 'Is schema-constrained output supported, and what does validation reject?', 'Are reviewed training examples available, and how will duplicates be split?'],
    conciseAnswer: 'I would first address the measured causes: current retrieval for missing policy evidence, explicit contracts and validation for format errors, and controlled prompt/model experiments for reasoning failures. Fine-tuning is a candidate for a remaining repeatable behavior gap with suitable data, not the default solution for changing facts. I would evaluate each intervention separately against held-out cases.',
    reasoning: ['Preserve the failure taxonomy and avoid making simultaneous changes that hide causality.', 'Version the corpus and check freshness and citation support for weekly policy changes.', 'Require strict validation even if a model is tuned or produces constrained JSON.', 'Split near-duplicates by source or time and compare the whole system to the baseline.'],
    followUp: 'A smaller model is cheaper. Can we route using its confidence field and send only low-confidence answers to a larger model?',
    followUpAnswer: 'Only after evaluating and calibrating that routing rule. Self-reported confidence can be wrong, especially on hard or unfamiliar cases. Measure false confidence, escalation frequency, full-system quality, and end-to-end cost/latency on held-out data. Add deterministic checks where possible; do not infer router quality from the two models’ standalone averages.',
    pitfalls: ['Using tuning to solve freshness or authorization.', 'Leaking template duplicates into the test set.', 'Treating model confidence as calibrated probability.'],
    rubric: ['Maps interventions to measured causes.', 'Keeps weekly facts in a fresh evidence path.', 'Explains training-data and validation requirements.', 'Evaluates the complete routing system.'],
  },
  {
    id: 'cache-review', workshopId: 'advanced-incidents', title: 'Review an AI-generated cache optimization', level: 'Senior · coding/debugging', minutes: 10,
    prompt: 'Review this optimization: const key = request.question; const hit = cache.get(key); if (hit) return hit; A test with one user passes. Two tenants ask the same question about their private study plans. Explain the regression, smallest fix, and incident response.',
    clarifications: ['Is the cache shared across tenants or users?', 'Is authorization derived from trusted session state?', 'How are corpus updates and permission revocations propagated?'],
    conciseAnswer: 'The cache key lacks authorization scope, so one tenant can receive another’s answer. I would contain the issue by disabling and invalidating unsafe shared entries, derive cache scope from authenticated identity and effective access, and include relevant content and model/prompt versions. I would add a two-tenant identical-question test that fails on the original code, then check revocation and version changes.',
    reasoning: ['Start with containment and minimal safe evidence; avoid broad private prompt dumps.', 'Define a canonical key from trusted access scope and relevant versions, not a body-supplied tenant ID.', 'Recheck authorization as required and invalidate on revocation; inspect retrieval boundaries too.', 'Run old and new tests, review the minimal diff, and monitor a staged re-enable with rollback.'],
    followUp: 'A developer adds tenantId from the JSON request body to the key. Is that sufficient?',
    followUpAnswer: 'No. A caller may choose another tenant ID. Derive identity and effective permissions from authenticated server state, and account for user-level access differences inside a tenant. Test a spoofed body tenant, revoked access, and stale cached entries. Key changes alone also do not remove unsafe old entries or establish that uncached retrieval is authorized.',
    pitfalls: ['Trusting body-supplied tenant identity.', 'Fixing the key but retaining leaked entries.', 'Accepting a one-user generated test as evidence of isolation.'],
    rubric: ['Names the concrete cross-tenant regression.', 'Uses trusted effective authorization scope.', 'Includes invalidation and revocation tests.', 'Explains containment, evidence, and rollback.'],
  },
];

// A wall-clock deadline handles background-tab timer throttling without counting interval ticks.
export function remainingInterviewSeconds(deadlineMs, nowMs) {
  return Math.max(0, Math.ceil((deadlineMs - nowMs) / 1000));
}
export function interviewSelfAssessment(rubric, checked) {
  const covered = rubric.filter((_, index) => checked.includes(index));
  return { covered: covered.length, total: rubric.length, revisit: rubric.filter((_, index) => !checked.includes(index)) };
}
